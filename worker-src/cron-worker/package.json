{
  "name": "chesscom-helper-cron-worker",
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}